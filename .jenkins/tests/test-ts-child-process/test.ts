console.log('SUCCESS!!');

// exec('echo YEY!!');
// exec('export NVM_DIR="$HOME/.nvm"\n' +
// 	'[ -s "$NVM_DIR/nvm.sh" ] && . "$NVM_DIR/nvm.sh"  # This loads nvm\n' +
// 	'[ -s "$NVM_DIR/bash_completion" ] && . "$NVM_DIR/bash_completion"  # This loads nvm bash_completion', {shell: '/bin/bash'});